# Runtime dependencies — mirrors pyproject.toml [project.dependencies].
# Hand-maintained; keep in sync with pyproject.toml on every edit.

# Live data
aiohttp>=3.9.0
websockets>=12.0

# Auth
cryptography>=42.0.0
python-dotenv>=1.0.0
requests>=2.31.0

# Numerics + regression
numpy>=1.24.0
pandas>=2.1.0
scikit-learn>=1.3.0
